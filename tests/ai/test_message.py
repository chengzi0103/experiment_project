# Generated by CodiumAI

from aiverse.ai.message import create_chat_message

import pytest


class TestCodeUnderTest:

    #  Function creates a chat message with default values.
    def test_default_values(self):
        # Initialize the code under test

        # Invoke the code under test
        result = create_chat_message()

        # Check the result
        assert result['role'] == 'user'
        assert result['content'] == '你好，请你用生动的话语给我讲一个小故事吧'

    #  Function creates a chat message with role and content exceeding maximum length.
    def test_exceed_max_length(self):
        # Initialize the code under test

        # Invoke the code under test with role and content exceeding maximum length
        result = create_chat_message(role='a' * 256, content='b' * 256)

        # Check the result
        assert len(result['role']) <= 255
        assert len(result['content']) <= 255
